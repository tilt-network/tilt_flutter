## 0.1.0

* Initial release.
* Android peer SDK bridge via `Tilt(publicKey, environment: env)`.
* Live log streaming through `EventChannel('tilt/logs')`.
* Foreground service lifecycle managed automatically.
